export { createTaggerPlugin } from "./plugin";
export type {
  TaggerPluginOptions,
  ComponentInfo,
  PluginStats,
  JSXAttributeMap,
} from "./types";

// Default export for convenience
export { createTaggerPlugin as default } from "./plugin";
